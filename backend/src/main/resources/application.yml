server:
  address: 0.0.0.0
  port: 8090
  servlet:
    context-path: /admin

spring:
  profiles:
    active: dev   # 切换成 prod 可用于上线环境  切换成 dev 可用于本地环境  但是本地环境需要看本地ip
    
  datasource:
    url: jdbc:mysql://rm-2zereaqi1k536nd38zo.mysql.rds.aliyuncs.com:3306/lovecube?useSSL=false&serverTimezone=Asia/Shanghai
    username: xifg
    password: Lj962464!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 0ms
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2

  session:
    store-type: redis
    timeout: 1800s

  cache:
    type: redis

  security:
    user:
      name: admin
      password: admin123

  web:
    cors:
      allowed-origins: "*"
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
      allowed-headers: "*"
      allow-credentials: true
      exposed-headers: Authorization

  elasticsearch:
    uris: http://localhost:9200
    username: elastic
    password: changeme

logging:
  level:
    root: info
    org.hibernate.SQL: off
    org.hibernate.type.descriptor.sql.BasicBinder: off

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

wechat:
  appid: wx4fd72b722c677342
  secret: 5dd1e246d84e0ff739338509fd952596
  api:
    login:
      path: /api/wechat/login

